# OS / Editor
.DS_Store
.idea
.vscode

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Cursor / AI
.cursorrules

# Temp
.tmp

# Node / Web (if any Node tooling remains)
node_modules/

# Go build artifacts (DO NOT COMMIT)
bin/
small

# SMALL runtime artifacts (never commit)
.small/

# --- EXCEPTIONS: spec content is canonical and SHOULD be committed ---

# Unignore spec tree so nested negations can work
!spec/
!spec/**/

# Unignore any .small directories under spec (if you store canonical artifacts there)
!spec/**/.small/
!spec/**/.small/**

# Unignore canonical SMALL artifact files if they exist under spec
!spec/**/intent.small.yml
!spec/**/constraints.small.yml
!spec/**/plan.small.yml
!spec/**/progress.small.yml
!spec/**/handoff.small.yml